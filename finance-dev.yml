spring:
  application:
    name: account
    index: 0

  datasource:
    url: jdbc:mysql://${CONFIG_SERVER_IP:192.168.0.9}:3306/xt_finance_service
    username: root
    password: xiey
    testWhileIdle: true
    validationQuery: SELECT 1

  cloud:
    stream:
      instanceCount: 1
      instanceIndex: 0
      bindings:
        input:
          # destination: testq1
          group: finance
          consumer:
            concurrency: 1
            partitioned: false
            headerMode: raw
        output:
          # destination: testq2
          group: finance
          producer:
            # partitionKeyExpression: null
            # partitionKeyExtractorClass: null
            # partitionSelectorExpression: null
            # partitionSelectorClass: null
            partitionCount: 1
            headerMode: raw





